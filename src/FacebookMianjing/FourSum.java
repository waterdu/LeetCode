package FacebookMianjing;

import java.util.*;

/**
 * You're given an array of integers(eg [3,4,7,1,2,9,8]) Find the index of values that satisfy A+B = C + D, where
 * A,B,C & D are integers values in the array. Eg: Given [3,4,7,1,2,9,8] array The following 3+7 = 1+ 9 satisfies
 * A+B=C+D so print (0,2,3,5)
 */
//public class FourSum {//变种 four sum题
//    //Brute force approach will be to simply iterate over the array 4 times to generate all possible combinations and
//    // then verify where we get a tuple satisfying all the condition listed above or not. It looks like:
//    public List<List<Integer>> (int[] array) {
//        ArrayList<List<Integer>> result=new ArrayList<>();
//        if(array==null||array.length==0) return result;
//
//    }
//}
